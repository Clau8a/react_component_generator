import React from "react";

const ExportDefault = ({ componentName }) => {
  return (
    <label>
      {`export default xd`}
      <b>{componentName}</b>;
    </label>
  );
};

export default ExportDefault;
